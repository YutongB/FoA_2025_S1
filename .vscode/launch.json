{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug current C file",
        "type": "lldb", // CodeLLDB extension
        "request": "launch",
        "program": "${fileDirname}/${fileBasenameNoExtension}",
        "args": [],
        "cwd": "${fileDirname}",
      }
    ]
  }
  